import React from "react";

interface LogoProps {}

export const Logo = (props: LogoProps) => {
  return (
    <div className="text-2xl font-semibold flex">
      <p className="text-indigo-400	font-mono">em</p>
      crav
    </div>
  );
};
